
CFLAGS = -Wall -Wextra -Iutil -Lutil

all: main 

main: main.c H.c util/libbitarr.a
	$(CC) $(CFLAGS) -Wc++-compat -o main main.c H.c -lbitarr

test: main 
	./main

clean:
	rm -rf main H *.o *.dSYM *.greg

.PHONY: all clean test
