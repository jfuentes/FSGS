
CFLAGS = -Wall -Wextra -Iutil -Lutil

all: main 

H.o: H.h

main: main.c util/libbitarr.a
	$(CC) $(CFLAGS) -Wc++-compat -o main main.c -lbitarr

test: main 
	./main

clean:
	rm -rf main H *.o *.dSYM *.greg

.PHONY: all clean test
